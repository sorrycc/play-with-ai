/** Groups `items` into an object of arrays, keyed by what `key` gives for each item. */
export function groupBy(items, key) {
	const groups = {};
	// TODO(card): fill `groups`. `key` is a property name or a function
	return groups;
}
